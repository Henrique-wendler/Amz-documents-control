# Public frontend configuration. Obtain environment-specific values from
# `npx supabase status` (development) or the isolated Cloud project (staging).
# Never place secret/service-role keys in variables prefixed with VITE_.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
